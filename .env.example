# Example environment variables for Pete's BBQ Ramona.
# Copy to .env.local and fill in real values. Never commit .env files.

# Public site URL (used for OG / sitemap canonical)
NEXT_PUBLIC_SITE_URL=https://petessd.com
